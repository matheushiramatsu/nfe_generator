[tool.pytest.ini_options]
pythonpath = ['.']
testpaths = ['tests']
filterwarnings = ['ignore:Using `httpx` with:DeprecationWarning']

[tool.ruff]
line-length = 110

[tool.ruff.lint]
select = ['E4', 'E7', 'E9', 'F', 'I']

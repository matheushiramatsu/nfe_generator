# Optional for docker compose. Local-only development credential; no production environment exists.
POSTGRES_PASSWORD=local_homologation_only
# Python without Docker: copy these values to backend/.env as needed.
DATABASE_URL=sqlite:///./nfe.db
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
REQUESTS_PER_MINUTE=240
# SCHEMA_DIRECTORY=/absolute/path/to/official/schema/package
